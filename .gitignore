node_modules
dist
.DS_Store
*.local
*.tsbuildinfo

# The source workbook carries the tabs with player emails and IDs in them
# (User_Start_Dates, the raw form responses). Keep it local; only the
# scrubbed JSON in src/data/ is committed.
/data/
